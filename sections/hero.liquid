<!-- Hero Section -->
<section class="hero full-width">
  <div class="hero__background">
    {% if section.settings.hero_image != blank %}
      {{ section.settings.hero_image | image_url: width: 2000 | image_tag: class: 'hero__background-image', alt: section.settings.hero_title }}
    {% else %}
      <img src="{{ 'hero.png' | asset_url }}" class="hero__background-image" alt="{{ section.settings.hero_title }}" />
    {% endif %}
    <div class="hero__background-overlay"></div>
  </div>

  <div class="hero__content">
    <div class="hero__text">
      {% if section.settings.hero_title != blank %}
        <h1 class="hero__title">
          {{ section.settings.hero_title }}
          {% if section.settings.hero_title_accent != blank %}
            <span class="hero__title-accent">{{ section.settings.hero_title_accent }}</span>
          {% endif %}
        </h1>
      {% endif %}

      {% if section.settings.hero_subtitle != blank %}
        <p class="hero__subtitle">{{ section.settings.hero_subtitle }}</p>
      {% endif %}

      <div class="hero__buttons">
        {% if section.settings.primary_button_text != blank %}
          <a href="{{ section.settings.primary_button_link | default: collections.all.url }}" class="hero__button hero__button--primary">
            {{ section.settings.primary_button_text }}
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
              <path d="M5 12h14m-7-7l7 7-7 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        {% endif %}

        {% if section.settings.secondary_button_text != blank %}
          <a href="{{ section.settings.secondary_button_link | default: pages['about-us'].url }}" class="hero__button hero__button--secondary">
            {{ section.settings.secondary_button_text }}
          </a>
        {% endif %}
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="hero__scroll-indicator">
    <div class="hero__scroll-mouse">
      <div class="hero__scroll-wheel"></div>
    </div>
  </div>
</section>

{% stylesheet %}
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    color: #fff;
  }

  .hero__background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -2;
  }

  .hero__background-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .hero__background-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #8B4513 0%, #D2B48C 50%, #8B4513 100%);
    background-image: 
      radial-gradient(circle at 20% 80%, rgba(255, 107, 53, 0.3) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(139, 69, 19, 0.4) 0%, transparent 50%);
  }

  .hero__background-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      135deg,
      rgba(0, 0, 0, 0.4) 0%,
      rgba(0, 0, 0, 0.2) 50%,
      rgba(0, 0, 0, 0.6) 100%
    );
    z-index: -1;
  }

  .hero__content {
    max-width: 800px;
    text-align: center;
    padding: 0 2rem;
    z-index: 1;
  }

  .hero__title {
    font-size: clamp(3rem, 8vw, 6rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    letter-spacing: -0.02em;
  }

  .hero__title-accent {
    color: #ff6b35;
    display: block;
    font-weight: 800;
  }

  .hero__subtitle {
    font-size: clamp(1.125rem, 3vw, 1.375rem);
    line-height: 1.6;
    margin-bottom: 2.5rem;
    opacity: 0.95;
    max-width: 650px;
    margin-left: auto;
    margin-right: auto;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    font-weight: 400;
    letter-spacing: 0.025em;
  }

  .hero__buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
    align-items: center;
  }

  .hero__button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 50px;
    transition: all 0.3s ease;
    font-size: 1rem;
    border: 2px solid transparent;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    letter-spacing: 0.025em;
  }

  .hero__button--primary {
    background-color: #ff6b35;
    color: #fff;
    border-color: #ff6b35;
  }

  .hero__button--primary:hover {
    background-color: #e55a2e;
    border-color: #e55a2e;
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(255, 107, 53, 0.3);
  }

  .hero__button--secondary {
    background-color: transparent;
    color: #fff;
    border-color: rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(10px);
  }

  .hero__button--secondary:hover {
    background-color: rgba(255, 255, 255, 0.1);
    border-color: #fff;
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(255, 255, 255, 0.2);
  }

  /* Scroll Indicator */
  .hero__scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 1;
  }

  .hero__scroll-mouse {
    width: 24px;
    height: 40px;
    border: 2px solid rgba(255, 255, 255, 0.5);
    border-radius: 12px;
    position: relative;
    animation: mouse-bounce 2s infinite;
  }

  .hero__scroll-wheel {
    width: 2px;
    height: 6px;
    background-color: rgba(255, 255, 255, 0.7);
    border-radius: 1px;
    position: absolute;
    top: 8px;
    left: 50%;
    transform: translateX(-50%);
    animation: wheel-move 2s infinite;
  }

  @keyframes mouse-bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-8px);
    }
    60% {
      transform: translateY(-4px);
    }
  }

  @keyframes wheel-move {
    0% {
      opacity: 1;
      top: 8px;
    }
    100% {
      opacity: 0;
      top: 24px;
    }
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .hero {
      min-height: 80vh;
      text-align: center;
    }

    .hero__content {
      padding: 0 1rem;
    }

    .hero__buttons {
      flex-direction: column;
      width: 100%;
    }

    .hero__button {
      width: 100%;
      max-width: 280px;
      justify-content: center;
    }

    .hero__scroll-indicator {
      bottom: 1rem;
    }
  }

  @media (max-width: 480px) {
    .hero__title {
      font-size: 2.5rem;
    }

    .hero__subtitle {
      font-size: 1rem;
      margin-bottom: 2rem;
    }

    .hero__button {
      padding: 0.875rem 1.5rem;
      font-size: 0.9rem;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:sections.hero.name",
  "tag": "section",
  "class": "section-hero",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.hero.settings.header_content.content"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "t:sections.hero.settings.hero_image.label"
    },
    {
      "type": "text",
      "id": "hero_title",
      "label": "t:sections.hero.settings.hero_title.label",
      "default": "Conquer with"
    },
    {
      "type": "text",
      "id": "hero_title_accent",
      "label": "t:sections.hero.settings.hero_title_accent.label",
      "default": "Confidence"
    },
    {
      "type": "textarea",
      "id": "hero_subtitle",
      "label": "t:sections.hero.settings.hero_subtitle.label",
      "default": "Discover fragrances that define your presence and elevate your essence."
    },
    {
      "type": "header",
      "content": "t:sections.hero.settings.header_buttons.content"
    },
    {
      "type": "text",
      "id": "primary_button_text",
      "label": "t:sections.hero.settings.primary_button_text.label",
      "default": "Shop Collection"
    },
    {
      "type": "url",
      "id": "primary_button_link",
      "label": "t:sections.hero.settings.primary_button_link.label"
    },
    {
      "type": "text",
      "id": "secondary_button_text",
      "label": "t:sections.hero.settings.secondary_button_text.label",
      "default": "Learn More"
    },
    {
      "type": "url",
      "id": "secondary_button_link",
      "label": "t:sections.hero.settings.secondary_button_link.label"
    }
  ],
  "presets": [
    {
      "name": "t:sections.hero.presets.hero.name"
    }
  ]
}
{% endschema %}